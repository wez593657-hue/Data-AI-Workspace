# 存储过程日期参数规则

适用范围：所有新增或修改的 CRM 业务存储过程。存量过程仅在后续进入需求开发流程时按本规则改造。

## 基准日期

- `V_SYSDAT` 是当前跑批快照日期，格式必须为 `YYYYMMDD`。
- `V_SYSDAT` 仅可用于输入校验、运行日志和源表快照等值关联，例如 `t.data_date = V_SYSDAT`。
- 不得直接基于 `V_SYSDAT` 使用 `TO_DATE`、`TRUNC`、`ADD_MONTHS`、`LAST_DAY` 或日期加减推导月、季、年等业务边界。

## 日期参数映射

每个相对业务日期必须声明语义明确的具名变量，并由 `sys_fun_deal_date(V_SYSDAT, n)` 初始化。不得在业务 SQL 中直接调用该函数，也不得复用语义不一致的日期参数。

| 编号 | 日期语义 |
|---|---|
| 1 | 上一日（跑批日期的上一日） |
| 2 | 上月末 |
| 3 | 上季末 |
| 4 | 上年末 |
| 5 | 上上日 |
| 6 | 上上月末 |
| 7 | 上上季末 |
| 8 | 上上年末 |
| 9 | 当月初 |
| 10 | 当月末 |
| 11 | 当季初 |
| 12 | 当季末 |
| 13 | 当年初 |
| 14 | 当年末 |
| 15 | 上月初 |
| 16 | 上季初 |
| 17 | 上年初 |
| 18 | 30 天承接窗口开始日 |
| 19 | 三年历史清理边界 |
| 20 | 1月前 |
| 21 | 6月前 |
| 22 | 近3个月前的月末 |
| 23 | 近6个月前的月末 |
| 24 | 近9个月前的月末 |
| 25 | 近12个月前的月末 |
| 26 | 365天动账窗口开始日 |
| 27 | 180天新客窗口开始日 |
| 28 | 次日（跑批日期的下一日） |
| 29 | 近一年 |

如需新的相对业务日期，使用未占用编号，并在使用前更新本规则、需求记忆卡片及审核证据。

## 新增与使用日期参数流程（强制）

所有存储过程中使用或新增日期参数，必须遵循以下流程：

1. **先查函数实现**：使用日期参数前，必须查阅 `data_assets/function/sys_fun_deal_date.sql` 的 `CASE p_pd` 实现代码，确认所需日期语义是否已有对应参数编号。
2. **已存在 → 直接使用**：若参数已存在，直接用具名变量 + `sys_fun_deal_date(V_SYSDAT, n)` 初始化，禁止在业务 SQL 中内联调用或自行推导。
3. **不存在 → 先同步函数，再登记使用**：若需要新增日期参数，必须：
   - 在 `sys_fun_deal_date.sql` 的 `CASE p_pd` 中同步新增 `WHEN n THEN` 分支并实现对应日期逻辑；
   - 在本规则"日期参数映射"表登记编号、语义；
   - 更新需求记忆卡片及审核证据；
   - 之后方可在存储过程中使用该参数。
4. **禁止只登记不实现**：任何在规则表中登记但未在函数中实现（或反之）的参数编号，均视为不合规；校验器 `validate_procedure_date_parameters.py` 会强制检查"存储过程中使用的每个参数编号均已在函数中实现"。

## 实现与审核

- 在 `DECLARE` 区声明参数，并在初始化区由函数赋值；变量名必须表达日期业务语义，并在行尾注释标注编号和日期含义。
- 源字段日期解析、按记录自身日期的计算可以使用 `TO_DATE` 或 DATE 运算；它们不能替代跑批基准日期的业务边界参数。
- 目标表中的日期字段必须输出 8 位 `YYYYMMDD`。DATE/TIMESTAMP 值使用 `TO_CHAR(value, 'YYYYMMDD')`；已校验的 `V_SYSDAT` 可以直接输出。
- 需求记忆卡片必须列出本过程实际使用的参数编号、变量名和业务含义。
- 过程审核必须逐项确认：具名参数、禁止的直接推导、目标日期格式和记忆卡片一致性。

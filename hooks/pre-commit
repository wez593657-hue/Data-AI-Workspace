#!/usr/bin/env python3
import sys
import subprocess

result = subprocess.run([sys.executable, 'scripts/pre_commit_hook.py', '--mode', 'quick'], capture_output=True, text=True)
print(result.stdout)
print(result.stderr)
sys.exit(result.returncode)
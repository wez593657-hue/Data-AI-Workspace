#!/usr/bin/env python3
import subprocess
import sys

root = subprocess.run(
    ['git', 'rev-parse', '--show-toplevel'], capture_output=True, text=True, check=True
).stdout.strip()
sys.exit(subprocess.run([sys.executable, 'scripts/pre_commit_hook.py', '--mode', 'quick'], cwd=root).returncode)

dsh 插件安装环境事实：web profile（C:\Users\mxt19\.dsh\profiles\web）是 pnpm workspace 根（pnpm-workspace.yaml 含 packages:[.]），裸 `dsh plugin --profile web add <pkg>` 必报 ERR_PNPM_ADDING_TO_ROOT；CLI 逐字转发 pnpm 参数，加 `-w` 即可绕过，或在该 profile 的 .npmrc 写 ignore-workspace-root-check=true。`dsh-annotation` 不在 npm registry（404，各作用域变体同样 404，疑为 git 托管）；本沙箱无法访问 github.com（curl/git ls-remote/GitHub API 全部失败，schannel SEC_E_NO_CREDENTIALS / HTTP 000），npm registry 正常。
§
本环境网络拓扑：schannel TLS（git / 系统 curl / .NET Invoke-RestMethod）对任何主机都失败（SEC_E_NO_CREDENTIALS 或 HTTP 000，连 registry.npmjs.org 也是 000）；Node 原生 TLS 正常。GitHub 子域可达性：github.com / api.github.com / codeload.github.com 可达（偶发抖动，retry 即过），raw.githubusercontent.com 恒不可达。沙箱禁止 Node 子进程管道（pnpm spawn git 报 EPERM），因此 `.git` / github: 形式的依赖装不了，但 `github.com/<owner>/<repo>/archive/refs/heads/<branch>.tar.gz` tarball 形式可装（pnpm 用 Node TLS 拉取，不 spawn git）。示例：dsh-annotation 实为 @omdsh-dev/dsh-annotation@1.3.16，以 tarball URL 加入 web profile 成功，dsh.bundle.patch: ./cordis.patch.yml。
§
睡眠户唤醒 v2.16.2 最终版（手工整理后）：睡眠户身份唯一来源 DWS_CUST_DORMANT_ACCOUT（3列：persn_legal_bk_code/data_date/cust_id；DDL 在 data_assets/ddl/dws/dws_cust_dormant_account.sql；上游真实表名就是 ACCOUT 拼写——勿再"修正"为 ACCOUNT）；过程内禁止按资产/交易重算睡眠户；月初取当日快照全量，月内以「当日快照存在且NOT EXISTS当月BASE」增量追加，清单只增不减；不做个人过滤（DORMANT 全量入清单）。步骤编号 '1'参数校验+快照就绪校验、'2'当月产品新增预聚合(TMP_ADS_SLEEP_WAKE_PROD)、'3'当月接触预聚合(TMP_ADS_SLEEP_CNTCT)、'4'身份基底(月初DORMANT全量/月内昨日身份继承)、'5'当日增量、'6'写入ADS目标表——余额/名称/等级/管户经理/ORG 以 LEFT JOIN 直接计算（缺失按0/空，ORG 不兜底），接触=当月有效接触 EXISTS(TMP3=CNTCT)、唤醒=当月产品新增 EXISTS(TMP2=PROD) 直接判定；无 UPDATE、无下线占位。统计过程逻辑不变仅版本同步。设计文档 requirements/睡眠户唤醒存储过程逻辑修改设计文档.md（v1.5）。
§
0068/0069 商户类映射（2026-08-18 用户确认）：商户号经 uepp_pay_mct_settle_account.cust_no 映射核心客户号、算到客户维度（与 0081 同模式）。Excel口径文档已同步至v4.3(2026-08-19 requirements/指标业务理解与技术口径.xlsx)：0073-0079客户维度重构+0068/0069已开发+法人行映射(机构码前2位12→1200/15→1500/18→1800/其余9999)+贷款源mbk_cust_log_loan已写入；0068技术口径=标签表ADS_CRM_R_CUST_LABLE.IS_NOT_BILL_RSV_VAL_MKNT='1'(非计算)，待确认该标签是否等价业务口径(资产门槛+近30天交易量)；更新前备份 .bak_20260819 保留。完整口径见 requirements/指标数据统计_记忆卡片.md v1.7。
§
0068/0069 商户类指标口径定案（2026-08-18 用户确认，待实施）：商户号经 uepp_pay_mct_settle_account.cust_no 映射核心客户号（对应 DWS_CUST_ASSE_LIAB.CUST_ID），资产/留存均算到客户维度（关联个人客户 AUM/活期，非商户结算存款）。0068 价值商户=资产门槛（小微月日均AUM≥2万或活期≥1万 / 个体≥5万或活期≥2.5万）+近30天交易≥10笔且≥100元；0069 留存率=年日均/年累计交易（年交易≥500元商户）。已确认满足开发条件，SQL 框架参照 0081（retention_rate.sql scope_merchant→cust_no→资产聚合结构）已出，待用户确认后写入存储过程。待确认：0069 分子取年日均存款还是AUM、0068 活期字段、近30天参数。
§
0068/0069 商户类指标已实施完成（2026-08-18，零新增过程文件）：0068 收单价值商户数并入 prc_ads_stat_indx_event_count.sql（GBK，7.5/7.6 A/B 块，与 0067 同标签表计数模式：scope→uepp_pay_mct_info 不按 mct_type 过滤→settle_account.cust_no→ADS_CRM_R_CUST_LABLE.IS_NOT_BILL_RSV_VAL_MKNT='1'→COUNT DISTINCT CUST_ID）；0069 扩展 prc_ads_stat_indx_retention_rate.sql（GBK，A/B 块结构同 0081：mct_type='personage'、年交易≥500 元，分子=DWS_CUST_ASSE_LIAB.DEPO_BAL bal_type='4' 年日均存款，分母=年累计交易量）。plan.sql 无新增步骤（0068 走既有 6B、0069 走既有 6C2）。曾短暂新建独立过程 val_merchant + plan 步骤 6C3，用户评审后已迁移删除（仓库惯例：一个过程管一类指标，0068 与 0067 同为标签表计数故归 event_count）。下游无白名单需登记。两指标均不区分小微/个体。 溯源更正：仓库不存在「ADS_CUST_LABEL_BILL_INFO」表，0068 收单商户标签字段全部已存在于 ADS_CRM_R_CUST_LABLE（0067 同源），无需补建 DDL。括号平衡 diff=0 已验；risk-check fast 失败为既有睡眠户 unittest（7失败/35错误，与本次无关）。待数据库验证。
§
0073~0079（用户口称8个）重做口径时，以 data_assets/reference_logic/业务口径(1).sql 和 表结构.xlsx 为唯一新源；原 zhbb.sql/旧映射不再可靠。T3031/MBK=手机银行，T3035/UEPP=收单。全部指标在营销活动/目标任务框架中按客户号（核心 CUST_ID）统计；收单业务源数据虽以 MCT_ID，0076~0079 必须通过 UEPP_PAY_MCT_SETTLE_ACCOUNT.CUST_NO（字段释义=核心客户号）映射后按客户去重/汇总。0074/0075/0078/0079 统计窗口为考核期累计 term_begin_date~v_sysdat；0076无需订单明细。贷款来源已确认是 mbk_cust_log_loan（非 MBK_CUST_LOG_LOGIN）；字段含 businesssum/channel/tran_status/tran_date。用户规定每次客户关联需带法人行号：交易机构前2位12→1200、15→1500、18→1800，其余9999；一个MCT_ID唯一对应一个cust_no，而一个cust_no可对应多个MCT_ID。当前需确认无交易机构字段的MBK流水及收单订单的法人行归属来源，未确认前不可猜测重写结果逻辑。
§
指标数据统计架构优化已实施P0+P1+P2(部分)+统一编码（2026-08-19）：已实施——N2 freeze_baseline 3.3 LEFT→INNER+删HAVING；目录11文件统一GBK+CRLF+无尾随空白；N3 aum_balance b/hb子查询加scope_member EXISTS裁剪；N10 org_rollup_publish闭包反向上卷(CONNECT BY PRIOR sup_org_id=org_id)+START WITH叶子裁剪(SUBSTR data_blng 'ORG_')；N4 product_baseline b改子查询+ADS_STAT_INDX_BASELINE_MEMBER EXISTS裁剪；N8 retention_rate收单订单按mct_id预聚合(mct_tx CTE, pay_time窗口全局V_YAR_BEGIN~V_DAY_END故安全)。暂缓/待确认——N9 trans_count流水预聚合因时间窗口BETWEEN term_begin_date(per-scope)无法按cust_no独立预聚合、语义不安全；N10分区交换因ads_stat_indx_data.sql DDL无PARTITION BY、需生产库分区DDL；N5 cust_upgrade临界CASE提取CTE收益仅维护性(CPU收益微小)故暂缓；N8索引需schema_change(uepp_pay_order_info仅主键order_id无mct_id/pay_time索引)。全部括号diff=0已验。方案文档 requirements/指标数据统计_架构与性能优化设计方案.md。
§
0066 个贷新形成不良贷款率已开发(2026-08-19)：文件=freeze_baseline.sql新增3.4段(期初基准：活动开始前一天 term_begin_date=V_NEXT_DAY 时冻结正常1/关注2账户写入TMP_STAT_INDX_LOAN_BASE，NOT EXISTS防重，活动结束保留)+retention_rate.sql新增2段(率值，并入6C2，与0081/0069同类率值：分母=scope客户∩基准余额合计、分子=期末DWD_ACCT_LOAN变不良3/4/5账户当前余额，ROUND(分子/分母*100,2)写AGGR)+dwd_tmp_stat_indx_loan_base.sql(DDL)；独立过程loan_bad_rate已删、plan无新增步骤(自动走步骤3的3.4段+6C2)。关键细节：余额字段=DWD_ACCT_LOAN.BAL(非LOAN_BAL)；结清账户不计分子、新开不计分母；(PERSN_LEGAL_BK_CODE,CUST_ID)复合键；A→DWD_MKT_TSK_INFO、B/O→DWS_CUST_LVL_INFO、B/M→DWD_CUST_MAN；freeze末尾强校验INDX清单不含0066(基准缺失输出NULL不报错，与0081/0069同级)。Excel口径文档v4.4，台账已开发33个。
§
指标数据统计 0066 已开发且过程重命名（2026-08-19 用户确认）：0066 个贷新形成不良贷款率=ROUND(分子/分母*100,2)；分母=scope∩期初基准(正常1/关注2)DWD_ACCT_LOAN账户余额，分子=期末变不良(3/4/5)账户当前余额(结清不计、新开不计)；余额字段=BAL(非LOAN_BAL)。实现(零新增过程)：期初基准在 freeze_baseline 3.4 段(活动开始前一天 term_begin_date=V_NEXT_DAY 冻结入 TMP_STAT_INDX_LOAN_BASE，保留不清理、plan步骤1不含该表)，率值并入 retention_rate 第2段(走既有6C2)，独立过程 loan_bad_rate 与plan步骤6C4已删。教训：A路径 JOIN DWD_MKT_TSK_INFO 必须带 (blng_type='O' AND mkt_persn_org=blng_id) OR (blng_type='M' AND mkt_persn=blng_id) 条件(缺失致机构/经理维度客户膨胀，freeze 3.4 与 retention 第2段已补)；插入新块须统一 CRLF+GBK。org_rollup_publish 已改名 plan_010，plan 新增步骤7调用(强校验+机构上卷+ADS发布)。TMP DDL 在 data_assets/ddl/tmp/：tmp_pro_ads_stat_indx_data.ddl 已有 SCOPE/BAL_AGGR/CUST_STATE/AGGR(含主键，勿拆分重建)，tmp_stat_indx_loan_base.sql 为0066基准表。Excel 口径文档 v4.4(2026-08-19) 台账33个已开发，0068标签口径待确认项保留。
